"use client"
import * as React from "react"

export default function FarcasterWrapper({ children }: { children: React.ReactNode }): JSX.Element {
  // Placeholder wrapper to keep layout stable; integrate Farcaster SDK here if needed
  return <>{children}</>
}
